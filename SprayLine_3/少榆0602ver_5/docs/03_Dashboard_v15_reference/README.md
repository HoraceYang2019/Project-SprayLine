# Dashboard v15 Reference

本資料夾放置 Dashboard v15 的前端參考檔，並已套用本專案最高命名規則：

- 產線值：`line_1`、`line_2`、`line_3`
- 產線欄位：`line_id`
- 壓力欄位：`pressure_bar`
- 流量欄位：`flow_rate_ml_min`

此 HTML 仍是 Dashboard v15 reference/mock，不代表已直接串接正式後端。  
正式後端 workflow 以本版 `schema/`、`webservices/dashboard_v15_service/`、`ontology/` 與 `knowledge/` 為準。
