# Knowledge / Threshold v15

Dashboard v15 / DB Schema v2 以三張 threshold 表為主：

- `filter_threshold`
- `nozzle_threshold`
- `process_threshold`

舊版 generic `threshold_config.template.json` 已移除。  
本資料夾只保留 Dashboard v15 方向的 threshold template，且不填入任何正式 threshold 假數值。
