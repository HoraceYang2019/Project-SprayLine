# Ontology 模組

- `core`：站點、實體資產、1 分鐘／3 分鐘訊號、資料品質。
- `database`：Schema v5 資料表。
- `service`：API、Service 與讀寫資料表關係。
- `dashboard`：管理者與工程端顯示區塊。
- `event_rule`：門檻規則與事件觸發。
- `troubleshooting`：原因對策概念。
- `knowledge`：門檻與元件－問題－解方實例。

使用 Protégé 時由 `SprayLine_ontology_index.ttl` 開啟，以載入全部模組。
