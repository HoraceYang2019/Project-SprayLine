# 0610 老師逐字稿要求對照表（少榆0616ver_4）

| 0610 老師要求 | 少榆0616ver_4 對應 | 狀態 |
|---|---|---|
| 每次上課前要把最新檔案丟 GitHub | 版本資料夾改為 `少榆0616ver_4`，可直接放進 `SprayLine_3/` | 待上傳 |
| 原因對策 service 不要只空模板 | `troubleshooting_service.py` + `rules/sensor_event_mapping.json` 對應 cause / response | 已做草案 |
| Issue type 改成 state / fault state | detection 結果含 `severity_state`、`issue_state`、`fault_state` | 已做 |
| Sensor 1Hz 改成 1min / 3min | MonitoringWorker 查 `query_sensor_1min` / `query_sensor_3min` | 已做 |
| 需要 Timer / Thread 定時檢查 DB | `monitoring_worker.py` 有 `run_forever()` 與 `run_monitoring_once()` | 已做 |
| 超過 threshold 後產生 alert_event | `threshold_evaluator.py` + `alert_event_writer.py` | 已做 |
| alert_event 要回寫 DB | 呼叫 `db_alert.insert_alert_event()` | 已做，待 PostgreSQL 實測 |
| 原因與對策要能對應 | 呼叫 `link_alert_cause()` / `link_alert_response()` | 已做，cause_id / response_id 仍待最終確認 |
| batch_station_status 要回寫 DB | 呼叫 `db_status.get_batch_station_status()` + `upsert_batch_station_status()` | 已做 |
| 產生 future prediction 並回寫 DB | `FutureService.save_future_prediction_result()` 呼叫 `db_future.insert_future_prediction_result()` | 已做，待 PostgreSQL 實測 |
| UI 不要 hard code，要能 call function | 新增 `docs/contracts/manager_engineer_ui_db_function_map.md` | 已補文件，不改 UI |
| Ontology 後續再調 | 0616ver_4 未大改 ontology，只保留與 rules/knowledge 對應 | 保留 |
| DataPreprocess / 聖堯假資料進 DB 後要能觸發 alert | 新增 DB 實測流程與 smoke test | 待實測 |
| 避免 Timer 每分鐘重複寫同一 alert | 新增 `duplicate_alert_guard.py` | 已補 |

## 目前不可宣稱的事項

```text
不可宣稱 UI 已串完。
不可宣稱已在 PostgreSQL 完成端到端實測，除非實際在余宇承電腦跑過。
不可宣稱 data_quality_flag / threshold table 最終規格已定案。
```
