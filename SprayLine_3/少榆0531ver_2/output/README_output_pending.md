# Output layer pending

此資料夾保留給 rules / inference 執行後產生的 inferred output。

目前不放假資料，也不手寫：

```text
SprayLine_runtime_inferred_sparql.ttl
```

未來必須由：

```text
ontology + knowledge + runtime observation + rules
```

實際推論後產生。
