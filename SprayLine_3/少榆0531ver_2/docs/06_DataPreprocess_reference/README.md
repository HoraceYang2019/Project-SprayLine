# DataPreprocess Reference

本資料夾納入 `Project-SprayLine-main_0531/DataPreprocess/spray0528` 的資料欄位，並已直接套用本專案命名規則。

## 已統一命名

- `station` → `line_id`
- `flow_rate_ml_min` → `flow_rate_ml_min`
- `pressure_bar` → `pressure_bar`

本資料夾不提供對應表版本，而是直接提供已整合命名後的欄位版本。
